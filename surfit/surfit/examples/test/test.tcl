load libsurfit[info sharedlibextension]

clear_data
if { [source map_area.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_cntr.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_defarea.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_flt.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_func.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_iso.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_task.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_trend.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_warea.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_wcntr.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_wfunc.tcl] == 0 } {  
	return 0
}

clear_data
if { [source map_wiso.tcl] == 0 } {
	return 0
}

clear_data
if { [source map_wtask.tcl] == 0 } {
	return 0
}


