/* ----------------------------------------------------------------------------
 * This file was automatically generated by SWIG (http://www.swig.org).
 * Version 1.3.13u-20020617-2210
 * 
 * This file is not intended to be easily readable and contains a number of 
 * coding conventions designed to improve portability and efficiency. Do not make
 * changes to this file unless you know what you are doing--modify the SWIG 
 * interface file instead. 
 * ----------------------------------------------------------------------------- */


#ifdef __cplusplus
template<class T> class SwigValueWrapper {
    T *tt;
public:
    inline SwigValueWrapper() : tt(0) { }
    inline ~SwigValueWrapper() { if (tt) delete tt; } 
    inline SwigValueWrapper& operator=(const T& t) { tt = new T(t); return *this; }
    inline operator T&() const { return *tt; }
    inline T *operator&() { return tt; }
};                                                    
#endif

/***********************************************************************
 * common.swg
 *
 *     This file contains generic SWIG runtime support for pointer
 *     type checking as well as a few commonly used macros to control
 *     external linkage.
 *
 * Author : David Beazley (beazley@cs.uchicago.edu)
 *
 * Copyright (c) 1999-2000, The University of Chicago
 * 
 * This file may be freely redistributed without license or fee provided
 * this copyright message remains intact.
 ************************************************************************/

#include <string.h>

#if defined(_WIN32) || defined(__WIN32__)
#       if defined(_MSC_VER)
#               if defined(STATIC_LINKED)
#                       define SWIGEXPORT(a) a
#                       define SWIGIMPORT(a) extern a
#               else
#                       define SWIGEXPORT(a) __declspec(dllexport) a
#                       define SWIGIMPORT(a) extern a
#               endif
#       else
#               if defined(__BORLANDC__)
#                       define SWIGEXPORT(a) a _export
#                       define SWIGIMPORT(a) a _export
#               else
#                       define SWIGEXPORT(a) a
#                       define SWIGIMPORT(a) a
#               endif
#       endif
#else
#       define SWIGEXPORT(a) a
#       define SWIGIMPORT(a) a
#endif

#ifdef SWIG_GLOBAL
#define SWIGRUNTIME(a) SWIGEXPORT(a)
#else
#define SWIGRUNTIME(a) static a
#endif

#ifdef __cplusplus
extern "C" {
#endif

typedef void *(*swig_converter_func)(void *);
typedef struct swig_type_info *(*swig_dycast_func)(void **);

typedef struct swig_type_info {
  const char             *name;                 
  swig_converter_func     converter;
  const char             *str;
  swig_dycast_func        dcast;
  struct swig_type_info  *next;
  struct swig_type_info  *prev;
  void                   *clientdata;	
} swig_type_info;

#ifdef SWIG_NOINCLUDE

SWIGIMPORT(swig_type_info *) SWIG_TypeRegister(swig_type_info *);
SWIGIMPORT(swig_type_info *) SWIG_TypeCheck(char *c, swig_type_info *);
SWIGIMPORT(void *)           SWIG_TypeCast(swig_type_info *, void *);
SWIGIMPORT(swig_type_info *) SWIG_TypeDynamicCast(swig_type_info *, void **);
SWIGIMPORT(swig_type_info *) SWIG_TypeQuery(const char *);
SWIGIMPORT(void)             SWIG_TypeClientData(swig_type_info *, void *);

#else

static swig_type_info *swig_type_list = 0;

/* Register a type mapping with the type-checking */
SWIGRUNTIME(swig_type_info *)
SWIG_TypeRegister(swig_type_info *ti)
{
  swig_type_info *tc, *head, *ret, *next;
  /* Check to see if this type has already been registered */
  tc = swig_type_list;
  while (tc) {
    if (strcmp(tc->name, ti->name) == 0) {
      /* Already exists in the table.  Just add additional types to the list */
      head = tc;
      next = tc->next;
      goto l1;
    }
    tc = tc->prev;
  }
  head = ti;
  next = 0;

  /* Place in list */
  ti->prev = swig_type_list;
  swig_type_list = ti;

  /* Build linked lists */
 l1:
  ret = head;
  tc = ti + 1;
  /* Patch up the rest of the links */
  while (tc->name) {
    head->next = tc;
    tc->prev = head;
    head = tc;
    tc++;
  }
  head->next = next;
  return ret;
}

/* Check the typename */
SWIGRUNTIME(swig_type_info *) 
SWIG_TypeCheck(char *c, swig_type_info *ty)
{
  swig_type_info *s;
  if (!ty) return 0;        /* Void pointer */
  s = ty->next;             /* First element always just a name */
  while (s) {
    if (strcmp(s->name,c) == 0) {
      if (s == ty->next) return s;
      /* Move s to the top of the linked list */
      s->prev->next = s->next;
      if (s->next) {
	s->next->prev = s->prev;
      }
      /* Insert s as second element in the list */
      s->next = ty->next;
      if (ty->next) ty->next->prev = s;
      ty->next = s;
      return s;
    }
    s = s->next;
  }
  return 0;
}

/* Cast a pointer up an inheritance hierarchy */
SWIGRUNTIME(void *) 
SWIG_TypeCast(swig_type_info *ty, void *ptr) 
{
  if ((!ty) || (!ty->converter)) return ptr;
  return (*ty->converter)(ptr);
}

/* Dynamic pointer casting. Down an inheritance hierarchy */
SWIGRUNTIME(swig_type_info *) 
SWIG_TypeDynamicCast(swig_type_info *ty, void **ptr) 
{
  swig_type_info *lastty = ty;
  if (!ty || !ty->dcast) return ty;
  while (ty && (ty->dcast)) {
     ty = (*ty->dcast)(ptr);
     if (ty) lastty = ty;
  }
  return lastty;
}

/* Search for a swig_type_info structure */
SWIGRUNTIME(swig_type_info *)
SWIG_TypeQuery(const char *name) {
  swig_type_info *ty = swig_type_list;
  while (ty) {
    if (ty->str && (strcmp(name,ty->str) == 0)) return ty;
    if (ty->name && (strcmp(name,ty->name) == 0)) return ty;
    ty = ty->prev;
  }
  return 0;
}

/* Set the clientdata field for a type */
SWIGRUNTIME(void)
SWIG_TypeClientData(swig_type_info *ti, void *clientdata) {
  swig_type_info *tc, *equiv;
  if (ti->clientdata) return;
  ti->clientdata = clientdata;
  equiv = ti->next;
  while (equiv) {
    if (!equiv->converter) {
      tc = swig_type_list;
      while (tc) {
	if ((strcmp(tc->name, equiv->name) == 0))
	  SWIG_TypeClientData(tc,clientdata);
	tc = tc->prev;
      }
    }
    equiv = equiv->next;
  }
}
#endif

#ifdef __cplusplus
}

#endif

/*
 * $Header$
 * 
 * swigtcl8.swg
 */

#include <tcl.h>
#include <stdlib.h>
#include <stdarg.h>
#include <ctype.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Constant table */

#define SWIG_TCL_INT     1
#define SWIG_TCL_FLOAT   2
#define SWIG_TCL_STRING  3
#define SWIG_TCL_POINTER 4
#define SWIG_TCL_BINARY  5

/* Constant information structure */
typedef struct swig_const_info {
    int type;
    char *name;
    long lvalue;
    double dvalue;
    void   *pvalue;
    swig_type_info **ptype;
} swig_const_info;

typedef int   (*swig_wrapper)(ClientData, Tcl_Interp *, int, Tcl_Obj *CONST []);
typedef int   (*swig_wrapper_func)(ClientData, Tcl_Interp *, int, Tcl_Obj *CONST []);
typedef char *(*swig_variable_func)(ClientData, Tcl_Interp *, char *, char *, int);
typedef void  (*swig_delete_func)(ClientData);

typedef struct swig_method { 
  const char     *name;
  swig_wrapper   method;
} swig_method;

typedef struct swig_attribute {
  const char     *name;
  swig_wrapper   getmethod;
  swig_wrapper   setmethod;
} swig_attribute;
  
typedef struct swig_class {
  const char         *name;
  swig_type_info   **type;
  swig_wrapper       constructor;
  void              (*destructor)(void *);
  swig_method        *methods;
  swig_attribute     *attributes;
  struct swig_class **bases;
} swig_class;

typedef struct swig_instance {
  Tcl_Obj       *thisptr;
  void          *thisvalue;
  swig_class   *classptr;
  int            destroy;
  Tcl_Command    cmdtok;
} swig_instance;

#ifdef SWIG_NOINCLUDE
SWIGEXPORT(char *)    SWIG_PackData(char *c, void *ptr, int sz);
SWIGEXPORT(char *)    SWIG_UnpackData(char *c, void *ptr, int sz);
SWIGEXPORT(int)       SWIG_ConvertPtrFromString(Tcl_Interp *, char *, void **, swig_type_info *,int flags);
SWIGEXPORT(int)       SWIG_ConvertPtr(Tcl_Interp *, Tcl_Obj *, void **, swig_type_info *, int flags);
SWIGEXPORT(int)       SWIG_ConvertPacked(Tcl_Interp *, Tcl_Obj *, void *, int sz, swig_type_info *, int flags);
SWIGEXPORT(void)      SWIG_MakePtr(char *, void *, swig_type_info *, int flags);
SWIGEXPORT(Tcl_Obj *) SWIG_NewPointerObj(void *, swig_type_info *, int flags);
SWIGEXPORT(Tcl_Obj *) SWIG_NewPackedObj(void *, int sz, swig_type_info *, int flags);
SWIGEXPORT(int)       SWIG_GetArgs(Tcl_Interp *, int, Tcl_Obj *CONST [], const char *, ...);
SWIGEXPORT(char *)    SWIG_PointerTypeFromString(char *c);
SWIGEXPORT(void)      SWIG_Acquire(void *ptr);
SWIGEXPORT(int)       SWIG_Disown(void *ptr);
SWIGEXPORT(void)      SWIG_InstallConstants(Tcl_Interp *interp, struct swig_const_info constants[]);
SWIGEXPORT(Tcl_Obj *) SWIG_GetConstant(const char *key);
SWIGEXPORT(Tcl_Obj *) SWIG_NewInstanceObj(Tcl_Interp *interp, void *, swig_type_info *, int flags);
SWIGEXPORT(int)       SWIG_ObjectConstructor(ClientData, Tcl_Interp *, int, Tcl_Obj *CONST objv[]);
SWIGEXPORT(int)       SWIG_MethodCommand(ClientData, Tcl_Interp *, int, Tcl_Obj *CONST objv[]);
SWIGEXPORT(void)      SWIG_ObjectDelete(ClientData);
#else

/* Pack binary data into a string */
SWIGRUNTIME(char *)
SWIG_PackData(char *c, void *ptr, int sz) {
  static char hex[17] = "0123456789abcdef";
  int i;
  unsigned char *u = (unsigned char *) ptr;
  register unsigned char uu;
  for (i = 0; i < sz; i++,u++) {
    uu = *u;
    *(c++) = hex[(uu & 0xf0) >> 4];
    *(c++) = hex[uu & 0xf];
  }
  return c;
}

/* Unpack binary data from a string */
SWIGRUNTIME(char *)
SWIG_UnpackData(char *c, void *ptr, int sz) {
  register unsigned char uu = 0;
  register int d;
  unsigned char *u = (unsigned char *) ptr;
  int i;
  for (i = 0; i < sz; i++, u++) {
    d = *(c++);
    if ((d >= '0') && (d <= '9'))
      uu = ((d - '0') << 4);
    else if ((d >= 'a') && (d <= 'f'))
      uu = ((d - ('a'-10)) << 4);
    d = *(c++);
    if ((d >= '0') && (d <= '9'))
      uu |= (d - '0');
    else if ((d >= 'a') && (d <= 'f'))
      uu |= (d - ('a'-10));
    *u = uu;
  }
  return c;
}

/* Convert a pointer value */
SWIGRUNTIME(int)
SWIG_ConvertPtrFromString(Tcl_Interp *interp, char *c, void **ptr, swig_type_info *ty, int flags)
{
  swig_type_info *tc;
  /* Pointer values must start with leading underscore */
  if (*c != '_') {
    *ptr = (void *) 0;
    if (strcmp(c,"NULL") == 0) return TCL_OK;
    Tcl_SetResult(interp, (char *) "Type error. Expected a pointer", TCL_STATIC);
    return TCL_ERROR;
  }
  c++;
  c = SWIG_UnpackData(c,ptr,sizeof(void *));
  if (ty) {
    tc = SWIG_TypeCheck(c,ty);
    if (!tc) {
      Tcl_SetResult(interp, (char *) "Type error. Expected ", TCL_STATIC);
      Tcl_AppendElement(interp, (char *) ty->name);
      return TCL_ERROR;
    }
    *ptr = SWIG_TypeCast(tc,(void *) *ptr);
  }
  flags = 0;
  return TCL_OK;
}

/* Convert a pointer value */
SWIGRUNTIME(int)
SWIG_ConvertPtr(Tcl_Interp *interp, Tcl_Obj *oc, void **ptr, swig_type_info *ty, int flags)
{
  return SWIG_ConvertPtrFromString(interp, Tcl_GetStringFromObj(oc,NULL), ptr, ty, flags);
}

/* Convert a pointer value */
SWIGRUNTIME(char *)
SWIG_PointerTypeFromString(char *c) {
  char d;
  /* Pointer values must start with leading underscore. NULL has no type */
  if (*c != '_') {
    return 0;
  }
  c++;
  /* Extract hex value from pointer */
  while ((d = *c)) {
    if (!(((d >= '0') && (d <= '9')) || ((d >= 'a') && (d <= 'f')))) break;
    c++;
  }
  return c;
}

/* Convert a packed value value */
SWIGRUNTIME(int)
SWIG_ConvertPacked(Tcl_Interp *interp, Tcl_Obj *obj, void *ptr, int sz, swig_type_info *ty, int flags) {
  swig_type_info *tc;
  char  *c;
  
  if (!obj) goto type_error;
  c = Tcl_GetStringFromObj(obj,NULL);
  /* Pointer values must start with leading underscore */
  if (*c != '_') goto type_error;
  c++;
  c = SWIG_UnpackData(c,ptr,sz);
  if (ty) {
    tc = SWIG_TypeCheck(c,ty);
    if (!tc) goto type_error;
  }
  return TCL_OK;

type_error:

  if (flags) {
    if (ty) {
      Tcl_SetResult(interp, (char *) "Type error. Expected ", TCL_STATIC);
      Tcl_AppendElement(interp, (char *) ty->name);
      return TCL_ERROR;
    } else {
      Tcl_SetResult(interp, (char *) "Expected packed data.", TCL_STATIC);
      return TCL_ERROR;
    }
  }
  return TCL_ERROR;
}


/* Take a pointer and convert it to a string */
SWIGRUNTIME(void) 
SWIG_MakePtr(char *c, void *ptr, swig_type_info *ty, int flags) {
  if (ptr) {
    *(c++) = '_';
    c = SWIG_PackData(c,&ptr,sizeof(void *));
    strcpy(c,ty->name);
  } else {
    strcpy(c,(char *)"NULL");
  }
  flags = 0;
}

/* Create a new pointer object */
SWIGRUNTIME(Tcl_Obj *)
SWIG_NewPointerObj(void *ptr, swig_type_info *type, int flags) {
  Tcl_Obj *robj;
  char result[512];
  SWIG_MakePtr(result,ptr,type,flags);
  robj = Tcl_NewStringObj(result,-1);
  return robj;
}

SWIGRUNTIME(Tcl_Obj *)
SWIG_NewPackedObj(void *ptr, int sz, swig_type_info *type, int flags) {
  char result[1024];
  char *r = result;
  if ((2*sz + 1 + strlen(type->name)) > 1000) return 0;
  *(r++) = '_';
  r = SWIG_PackData(r,ptr,sz);
  strcpy(r,type->name);
  flags = 0;
  return Tcl_NewStringObj(result,-1);
}

static Tcl_HashTable   swigconstTable;
static int             swigconstTableinit = 0;

/* Install Constants */
SWIGRUNTIME(void)
SWIG_InstallConstants(Tcl_Interp *interp, swig_const_info constants[]) {
  int i;
  Tcl_Obj *obj;
  Tcl_HashEntry *entryPtr;
  int            newobj;

  if (!swigconstTableinit) {
    Tcl_InitHashTable(&swigconstTable, TCL_STRING_KEYS);
    swigconstTableinit = 1;
  }
  for (i = 0; constants[i].type; i++) {
    switch(constants[i].type) {
    case SWIG_TCL_INT:
      obj = Tcl_NewIntObj(constants[i].lvalue);
      break;
    case SWIG_TCL_FLOAT:
      obj = Tcl_NewDoubleObj(constants[i].dvalue);
      break;
    case SWIG_TCL_STRING:
      obj = Tcl_NewStringObj((char *) constants[i].pvalue,-1);
      break;
    case SWIG_TCL_POINTER:
      obj = SWIG_NewPointerObj(constants[i].pvalue, *(constants[i]).ptype,0);
      break;
    case SWIG_TCL_BINARY:
      obj = SWIG_NewPackedObj(constants[i].pvalue, constants[i].lvalue, *(constants[i].ptype),0);
      break;
    default:
      obj = 0;
      break;
    }
    if (obj) {
      Tcl_ObjSetVar2(interp,Tcl_NewStringObj(constants[i].name,-1), NULL, obj, TCL_GLOBAL_ONLY);
      entryPtr = Tcl_CreateHashEntry(&swigconstTable, constants[i].name, &newobj);
      Tcl_SetHashValue(entryPtr, (ClientData) obj);
    }
  }
}

SWIGRUNTIME(Tcl_Obj *)
SWIG_GetConstant(const char *key) {
  Tcl_HashEntry *entryPtr;
  if (!swigconstTableinit) return 0;
  entryPtr = Tcl_FindHashEntry(&swigconstTable, key);
  if (entryPtr) {
    return (Tcl_Obj *) Tcl_GetHashValue(entryPtr);
  }
  printf("Searching %s\n", key);
  return 0;
}

/* Get arguments */
SWIGRUNTIME(int)
SWIG_GetArgs(Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[], const char *fmt, ...)
{
  int        argno = 0, opt = 0, tempi;
  double     tempd;
  const char *c;
  va_list    ap;
  void      *vptr;
  Tcl_Obj   *obj = 0;
  swig_type_info *ty;

  va_start(ap,fmt);
  for (c = fmt; (*c && (*c != ':') && (*c != ';')); c++,argno++) {
    if (*c == '|') {
      opt = 1;
      c++;
    }
    if (argno >= (objc-1)) {
      if (!opt) {
	Tcl_SetResult(interp, (char *) "Wrong # args. ", TCL_STATIC);
	goto argerror;
      } else {
	va_end(ap);
	return TCL_OK;
      }
    }

    vptr = va_arg(ap,void *);
    if (vptr) {
      if (isupper(*c)) {
	obj = SWIG_GetConstant(Tcl_GetStringFromObj(objv[argno+1],0));
	if (!obj) obj = objv[argno+1];
      } else {
	obj = objv[argno+1];
      }
      switch(*c) {
      case 'i': case 'I':
      case 'l': case 'L':
      case 'h': case 'H':
      case 'b': case 'B':
	if (Tcl_GetIntFromObj(interp,obj,&tempi) != TCL_OK) goto argerror;
	if ((*c == 'i') || (*c == 'I')) *((int *)vptr) = tempi;
	else if ((*c == 'l') || (*c == 'L')) *((long *)vptr) = tempi;
	else if ((*c == 'h') || (*c == 'H')) *((short*)vptr) = tempi;
	else if ((*c == 'b') || (*c == 'B')) *((unsigned char *)vptr) = tempi;
	break;
      case 'f': case 'F':
      case 'd': case 'D':
	if (Tcl_GetDoubleFromObj(interp,obj,&tempd) != TCL_OK) goto argerror;
	if ((*c == 'f') || (*c == 'F')) *((float *) vptr) = (float)tempd;
	else if ((*c == 'd') || (*c == 'D')) *((double*) vptr) = tempd;
	break;
      case 's': case 'S':
	if (*(c+1) == '#') {
	  int *vlptr = (int *) va_arg(ap, void *);
	  *((char **) vptr) = Tcl_GetStringFromObj(obj, vlptr);
	  c++;
	} else {
	  *((char **)vptr) = Tcl_GetStringFromObj(obj,NULL);
	}
	break;
      case 'c': case 'C':
	*((char *)vptr) = *(Tcl_GetStringFromObj(obj,NULL));
	break;
      case 'p': case 'P':
	ty = (swig_type_info *) va_arg(ap, void *);
	if (SWIG_ConvertPtr(interp, obj, (void **) vptr, ty,0) == TCL_ERROR) goto argerror;
	break;
      case 'o': case 'O':
	*((Tcl_Obj **)vptr) = objv[argno+1];
	break;
      default:
	break;
      }
    }
  }
  
  if ((*c != ';') && ((objc-1) > argno)) {
    Tcl_SetResult(interp, (char *) "Wrong # args.", TCL_STATIC);
    goto argerror;
  }
  va_end(ap);
  return TCL_OK;
  
 argerror:
  {
    char temp[32];
    sprintf(temp,"%d", argno+1);
    c = strchr(fmt,':');
    if (!c) c = strchr(fmt,';');
    if (!c) c = (char *)"";
    Tcl_AppendResult(interp,c," argument ", temp, NULL);
    va_end(ap);
    return TCL_ERROR;
  }
}

/* Object support */
static Tcl_HashTable  swigobjectTable;
static int            swigobjectTableinit = 0;

/* Acquire ownership of a pointer */
SWIGRUNTIME(void)
SWIG_Acquire(void *ptr)
{
  Tcl_HashEntry *entryPtr;
  int newobj;
  if (!swigobjectTableinit) {
    Tcl_InitHashTable(&swigobjectTable, TCL_ONE_WORD_KEYS);
    swigobjectTableinit = 1;
  }
  entryPtr = Tcl_CreateHashEntry(&swigobjectTable, (char *) ptr, &newobj);
}

/* Disown a pointer.  Returns 1 if we owned it to begin with */
SWIGRUNTIME(int)
SWIG_Disown(void *ptr)
{
  Tcl_HashEntry *entryPtr;
  if (!swigobjectTableinit) return 0;
  entryPtr = Tcl_FindHashEntry(&swigobjectTable, (char *) ptr);
  if (entryPtr) {
    Tcl_DeleteHashEntry(entryPtr);
    return 1;
  } 
  return 0;
}

SWIGRUNTIME(void)
SWIG_ObjectDelete(ClientData clientData) {
  swig_instance *si = (swig_instance *) clientData;
  if ((si) && (si->destroy) && (SWIG_Disown(si->thisvalue))) {
    if (si->classptr->destructor) {
      (si->classptr->destructor)(si->thisvalue);
    }
  }
  Tcl_DecrRefCount(si->thisptr);
  free(si);
}

/* Function to invoke object methods given an instance */
SWIGRUNTIME(int)
SWIG_MethodCommand(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST _objv[])
{
  char *method,   *attrname;
  swig_instance   *inst = (swig_instance *) clientData;
  swig_method     *meth;
  swig_attribute  *attr;
  Tcl_Obj         *oldarg;
  Tcl_Obj         **objv;
  int              rcode;
  swig_class      *cls;
  swig_class      *cls_stack[64];
  int              cls_stack_bi[64];
  int              cls_stack_top = 0;
  int              numconf = 2;
  int              bi;

  objv = (Tcl_Obj **) _objv;
  if (objc < 2) {
    Tcl_SetResult(interp, (char *) "wrong # args.", TCL_STATIC);
    return TCL_ERROR;
  }
  method = Tcl_GetStringFromObj(objv[1],NULL);
  if (strcmp(method,"-acquire") == 0) {
    inst->destroy = 1;
    SWIG_Acquire(inst->thisvalue);
    return TCL_OK;
  } 
  if (strcmp(method,"-disown") == 0) {
    if (inst->destroy) {
      SWIG_Disown(inst->thisvalue);
    }
    inst->destroy = 0;
    return TCL_OK;
  }
  if (strcmp(method,"-delete") == 0) {
    Tcl_DeleteCommandFromToken(interp,inst->cmdtok);
    return TCL_OK;
  }
  cls_stack[cls_stack_top] = inst->classptr;
  cls_stack_bi[cls_stack_top] = -1;
  cls = inst->classptr;
  while (1) {
    bi = cls_stack_bi[cls_stack_top];
    cls = cls_stack[cls_stack_top];
    if (bi != -1) {
      cls = cls->bases[bi];
      if (cls) {
	cls_stack_bi[cls_stack_top]++;
	cls_stack_top++;
	cls_stack[cls_stack_top] = cls;
	cls_stack_bi[cls_stack_top] = -1;
	continue;
      }
    }
    if (!cls) {
      cls_stack_top--;
      if (cls_stack_top < 0) break;
      else continue;
    }
    cls_stack_bi[cls_stack_top]++;

    meth = cls->methods;
    /* Check for methods */
    while (meth && meth->name) {
      if (strcmp(meth->name,method) == 0) {
	oldarg = objv[1];
	objv[1] = inst->thisptr;
	Tcl_IncrRefCount(inst->thisptr);
	rcode = (*meth->method)(clientData,interp,objc,objv);
	objv[1] = oldarg;
	Tcl_DecrRefCount(inst->thisptr);
	return rcode;
      }
      meth++;
    }
    /* Check class methods for a match */
    if (strcmp(method,"cget") == 0) {
      if (objc < 3) {
	Tcl_SetResult(interp, (char *) "wrong # args.", TCL_STATIC);
	return TCL_ERROR;
      }
      attrname = Tcl_GetStringFromObj(objv[2],NULL);
      attr = cls->attributes;
      while (attr && attr->name) {
	if ((strcmp(attr->name, attrname) == 0) && (attr->getmethod)) {
	  oldarg = objv[1];
	  objv[1] = inst->thisptr;
	  Tcl_IncrRefCount(inst->thisptr);
	  rcode = (*attr->getmethod)(clientData,interp,2, objv);
	  objv[1] = oldarg;
	  Tcl_DecrRefCount(inst->thisptr);
	  return rcode;
	}
	attr++;
      }
      if (strcmp(attrname, "-this") == 0) {
	Tcl_SetObjResult(interp, Tcl_DuplicateObj(inst->thisptr));
	return TCL_OK;
      }
    } else if (strcmp(method, "configure") == 0) {
      int i;
      if (objc < 4) {
	Tcl_SetResult(interp, (char *) "wrong # args.", TCL_STATIC);
	return TCL_ERROR;
      }
      i = 2;
      while (i < objc) {
	attrname = Tcl_GetStringFromObj(objv[i],NULL);
	attr = cls->attributes;
	while (attr && attr->name) {
	  if ((strcmp(attr->name, attrname) == 0) && (attr->setmethod)) {
	    oldarg = objv[i];
	    objv[i] = inst->thisptr;
	    Tcl_IncrRefCount(inst->thisptr);
	    rcode = (*attr->setmethod)(clientData,interp,3, &objv[i-1]);
	    objv[i] = oldarg;
	    Tcl_DecrRefCount(inst->thisptr);
	    if (rcode != TCL_OK) return rcode;
	    numconf += 2;
	  }
	  attr++;
	}
	i+=2;
      }
    }
  }
  if (strcmp(method,"configure") == 0) {
    if (numconf >= objc) {
      return TCL_OK;
    } else {
      Tcl_SetResult(interp,(char *) "Invalid attribute name.", TCL_STATIC);
      return TCL_ERROR;
    }
  }
  if (strcmp(method,"cget") == 0) {
      Tcl_SetResult(interp,(char *) "Invalid attribute name.", TCL_STATIC);
      return TCL_ERROR;
  }

  Tcl_SetResult(interp, (char *) "Invalid method. Must be one of: configure cget -acquire -disown -delete", TCL_STATIC);
  cls = inst->classptr;
  bi = 0;
  while (cls) {
    meth = cls->methods;
    while (meth && meth->name) {
      char *cr = (char *) Tcl_GetStringResult(interp);
      if (!strstr(strchr(cr,':'), meth->name))
	Tcl_AppendElement(interp, (char *) meth->name);
      meth++;
    }
    cls = inst->classptr->bases[bi++];
  }
  return TCL_ERROR;
}

/* Function to create objects */
SWIGRUNTIME(int)
SWIG_ObjectConstructor(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[])
{
    Tcl_Obj          *newObj = 0;
    void             *thisvalue = 0;
    swig_instance   *newinst = 0;
    swig_class      *classptr = (swig_class *) clientData;
    swig_wrapper     cons = 0;
    char             *name = 0;
    int               firstarg = 0;
    int               thisarg = 0;
    int               destroy = 1; 

    if (!classptr) {
      Tcl_SetResult(interp, (char *) "swig: internal runtime error. No class object defined.", TCL_STATIC);
      return TCL_ERROR;
    }
    cons = classptr->constructor;
    if (objc > 1) {
      char *s = Tcl_GetStringFromObj(objv[1],NULL);
      if (strcmp(s,"-this") == 0) {
	thisarg = 2;
	cons = 0;
      } else if (strcmp(s,"-args") == 0) {
	firstarg = 1;
      } else if (objc == 2) {
	firstarg = 1;
	name = s;
      } else if (objc >= 3) {
	char *s1;
	name = s;
	s1 = Tcl_GetStringFromObj(objv[2],NULL);
	if (strcmp(s1,"-this") == 0) {
	  thisarg = 3;
	  cons = 0;
	} else {
	  firstarg = 1;
	}
      }
    }
    if (cons) {
      int result;
      result = (*cons)(0, interp, objc-firstarg, &objv[firstarg]);
      if (result != TCL_OK) {
	return result;
      }
      newObj = Tcl_DuplicateObj(Tcl_GetObjResult(interp));
      if (!name) name = Tcl_GetStringFromObj(newObj,NULL);
    } else if (thisarg > 0) {
      if (thisarg < objc) {
	destroy = 0;
	newObj = Tcl_DuplicateObj(objv[thisarg]);
	if (!name) name = Tcl_GetStringFromObj(newObj,NULL);
      } else {
	Tcl_SetResult(interp, (char *) "wrong # args.", TCL_STATIC);
	return TCL_ERROR;
      }
    } else {
      Tcl_SetResult(interp, (char *) "No constructor available.", TCL_STATIC);
      return TCL_ERROR;
    }
    if (SWIG_ConvertPtr(interp,newObj, (void **) &thisvalue, *(classptr->type),0) == TCL_ERROR) {
      Tcl_DecrRefCount(newObj);
      return TCL_ERROR;
    }
    newinst = (swig_instance *) malloc(sizeof(swig_instance));
    newinst->thisptr = newObj;
    Tcl_IncrRefCount(newObj);
    newinst->thisvalue = thisvalue;
    newinst->classptr = classptr;
    newinst->destroy = destroy;
    if (destroy) {
      SWIG_Acquire(thisvalue);
    }
    newinst->cmdtok = Tcl_CreateObjCommand(interp,name, (swig_wrapper) SWIG_MethodCommand, (ClientData) newinst, (swig_delete_func) SWIG_ObjectDelete);
    return TCL_OK;
}


/* This function takes the current result and turns it into an object command */
SWIGRUNTIME(Tcl_Obj *)
SWIG_NewInstanceObj(Tcl_Interp *interp, void *thisvalue, swig_type_info *type, int flags) {
  Tcl_Obj *robj = SWIG_NewPointerObj(thisvalue, type,0);
  /* Check to see if this pointer belongs to a class or not */
  if ((type->clientdata) && (interp)) {
    Tcl_CmdInfo    ci;
    char          *name;
    name = Tcl_GetStringFromObj(robj,NULL);
    if (!Tcl_GetCommandInfo(interp,name, &ci) || (flags)) {
      swig_instance *newinst = (swig_instance *) malloc(sizeof(swig_instance));
      newinst->thisptr = Tcl_DuplicateObj(robj);
      Tcl_IncrRefCount(newinst->thisptr);
      newinst->thisvalue = thisvalue;
      newinst->classptr = (swig_class *) type->clientdata;
      newinst->destroy = flags;
      newinst->cmdtok = Tcl_CreateObjCommand(interp, Tcl_GetStringFromObj(robj,NULL), (swig_wrapper_func) SWIG_MethodCommand, (ClientData) newinst, (swig_delete_func) SWIG_ObjectDelete);
      if (flags) {
	SWIG_Acquire(thisvalue);
      }
    }
  }
  return robj;
}

#endif

/* Structure for command table */
typedef struct {
  const char *name;
  int       (*wrapper)(ClientData, Tcl_Interp *, int, Tcl_Obj *CONST []);
  ClientData  clientdata;
} swig_command_info;

/* Structure for variable linking table */
typedef struct {
  const char *name;
  void *addr;
  char * (*get)(ClientData, Tcl_Interp *, char *, char *, int);
  char * (*set)(ClientData, Tcl_Interp *, char *, char *, int);
} swig_var_info;

#ifdef __cplusplus
}
#endif






/* -------- TYPES TABLE (BEGIN) -------- */

#define  SWIGTYPE_p_surfit__ptask swig_types[0] 
#define  SWIGTYPE_p_surfit__parea swig_types[1] 
#define  SWIGTYPE_p_surfit__piso swig_types[2] 
#define  SWIGTYPE_p_surfit__pcntr swig_types[3] 
static swig_type_info *swig_types[5];

/* -------- TYPES TABLE (END) -------- */

#define SWIG_init    Freeflow_Init
#define SWIG_name    "freeflow"
#define SWIG_prefix  ""
#define SWIG_version "0.0"


#ifdef __cplusplus
extern "C" {
#endif
#ifdef MAC_TCL
#pragma export on
#endif
SWIGEXPORT(int) SWIG_init(Tcl_Interp *);
#ifdef MAC_TCL
#pragma export off
#endif
#ifdef __cplusplus
}
#endif



#include "real.h"
#include "flow_ie.h"
#include "flow.h"
#include "variables.h"
#include "flow_variables.h"
#include "ptask_tcl.h"
#include "ptasks.h"
#include "parea_tcl.h"
#include "pareas.h"
#include "pcntr_tcl.h"
#include "pcntrs.h"
#include "piso_tcl.h"
#include "pisos.h"
#include <float.h>


#include "lcm_simple_tcl.h"
#include "flow_func_tcl.h"


#include "flow_colors.h"
#include "ptask_show.h"
#include "parea_show.h"
#include "piso_show.h"
#include "pcntr_show.h"

extern int surfit::ptask_color_red;
extern int surfit::ptask_color_green;
extern int surfit::ptask_color_blue;
extern int surfit::ptask_color_alpha;
extern int surfit::draw_ptask;
extern int surfit::parea_color_red;
extern int surfit::parea_color_green;
extern int surfit::parea_color_blue;
extern int surfit::parea_color_alpha;
extern int surfit::draw_parea;
extern int surfit::piso_color_red;
extern int surfit::piso_color_green;
extern int surfit::piso_color_blue;
extern int surfit::piso_color_alpha;
extern int surfit::draw_piso;
extern int surfit::pcntr_color_red;
extern int surfit::pcntr_color_green;
extern int surfit::pcntr_color_blue;
extern int surfit::pcntr_color_alpha;
extern int surfit::draw_pcntr;
#ifdef __cplusplus
extern "C" {
#endif
static int
_wrap_flow(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    if (SWIG_GetArgs(interp, objc, objv,":flow ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            surfit::flow();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    
    return TCL_OK;
}

static int
_wrap_ptask_load(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|s:ptask_load filename ptaskname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_load((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_read(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    int arg3 = 3 ;
    int arg4 = 1 ;
    int arg5 = 2 ;
    int arg6 = 3 ;
    char *arg7 = " " ;
    int arg8 = 250 ;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|siiiisi:ptask_read filename ptaskname columns col1 col2 col3 delimiter grow_by ",&arg1,&arg2,&arg3,&arg4,&arg5,&arg6,&arg7,&arg8) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            surfit::ptask_read((char const *)arg1,(char const *)arg2,arg3,arg4,arg5,arg6,(char const *)arg7,arg8);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    
    return TCL_OK;
}

static int
_wrap_ptask_unload(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    if (SWIG_GetArgs(interp, objc, objv,":ptask_unload ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            surfit::ptask_unload();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    
    return TCL_OK;
}

static int
_wrap_ptask_save(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|s:ptask_save filename ptaskname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_save((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_write(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = " " ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|s:ptask_write filename delimiter ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_write((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_transform(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    double arg2 ;
    double arg3 ;
    double arg4 ;
    
    if (SWIG_GetArgs(interp, objc, objv,"dddd:ptask_transform shiftX scaleX shiftY scaleY ",&arg1,&arg2,&arg3,&arg4) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            surfit::ptask_transform(arg1,arg2,arg3,arg4);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    
    return TCL_OK;
}

static int
_wrap_ptask_inverse_transform(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    double arg2 ;
    double arg3 ;
    double arg4 ;
    
    if (SWIG_GetArgs(interp, objc, objv,"dddd:ptask_inverse_transform shiftX scaleX shiftY scaleY ",&arg1,&arg2,&arg3,&arg4) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            surfit::ptask_inverse_transform(arg1,arg2,arg3,arg4);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    
    return TCL_OK;
}

static int
_wrap_ptask_filter_by_defarea(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_filter_by_defarea ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_filter_by_defarea();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_filter_by_func(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:ptask_filter_by_func eps ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_filter_by_func(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_update_by_func(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_update_by_func ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_update_by_func();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_minx(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_minx ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (double)surfit::ptask_minx();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewDoubleObj((double) result));
    return TCL_OK;
}

static int
_wrap_ptask_maxx(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_maxx ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (double)surfit::ptask_maxx();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewDoubleObj((double) result));
    return TCL_OK;
}

static int
_wrap_ptask_miny(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_miny ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (double)surfit::ptask_miny();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewDoubleObj((double) result));
    return TCL_OK;
}

static int
_wrap_ptask_maxy(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_maxy ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (double)surfit::ptask_maxy();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewDoubleObj((double) result));
    return TCL_OK;
}

static int
_wrap_ptask_minz(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_minz ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (double)surfit::ptask_minz();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewDoubleObj((double) result));
    return TCL_OK;
}

static int
_wrap_ptask_maxz(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_maxz ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (double)surfit::ptask_maxz();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewDoubleObj((double) result));
    return TCL_OK;
}

static int
_wrap_ptask_add_noise(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:ptask_add_noise std ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_add_noise(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_mean(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_mean ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (double)surfit::ptask_mean();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewDoubleObj((double) result));
    return TCL_OK;
}

static int
_wrap_ptask_std(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    double result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:ptask_std mean ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (double)surfit::ptask_std(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewDoubleObj((double) result));
    return TCL_OK;
}

static int
_wrap_ptask_plus(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::ptask *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ptask_plus ptsk ",&arg1,SWIGTYPE_p_surfit__ptask) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_plus(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_minus(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::ptask *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ptask_minus ptsk ",&arg1,SWIGTYPE_p_surfit__ptask) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_minus(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_mult(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::ptask *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ptask_mult ptsk ",&arg1,SWIGTYPE_p_surfit__ptask) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_mult(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_div(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::ptask *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ptask_div ptsk ",&arg1,SWIGTYPE_p_surfit__ptask) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_div(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_set(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::ptask *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ptask_set ptsk ",&arg1,SWIGTYPE_p_surfit__ptask) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_set(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_plus_real(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:ptask_plus_real val ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_plus_real(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_minus_real(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:ptask_minus_real val ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_minus_real(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_mult_real(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:ptask_mult_real val ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_mult_real(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_div_real(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:ptask_div_real val ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_div_real(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_set_real(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:ptask_set_real val ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_set_real(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_to_task(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_to_task ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_to_task();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_to_tasks(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_to_tasks ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_to_tasks();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_task_to_ptask(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":task_to_ptask ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::task_to_ptask();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_task_to_ptasks(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":task_to_ptasks ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::task_to_ptasks();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_to_ptasks(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_to_ptasks ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_to_ptasks();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_check(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_check ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_check();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_size(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptask_size ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (int)surfit::ptask_size();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptask_name(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s:ptask_name new_name ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_name((char const *)arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptasks_load(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|s:ptasks_load filename ptaskname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptasks_load((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptasks_read(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    int arg3 = 3 ;
    int arg4 = 1 ;
    int arg5 = 2 ;
    int arg6 = 3 ;
    char *arg7 = " " ;
    int arg8 = 250 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|siiiisi:ptasks_read filename ptaskname columns col1 col2 col3 delimiter grow_by ",&arg1,&arg2,&arg3,&arg4,&arg5,&arg6,&arg7,&arg8) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptasks_read((char const *)arg1,(char const *)arg2,arg3,arg4,arg5,arg6,(char const *)arg7,arg8);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptasks_delall(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptasks_delall ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptasks_delall();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptasks_del(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"i:ptasks_del pos ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptasks_del(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptasks_at(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int arg1 ;
    surfit::ptask *result;
    
    if (SWIG_GetArgs(interp, objc, objv,"i:ptasks_at pos ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::ptask *)surfit::ptasks_at(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__ptask,0));
    return TCL_OK;
}

static int
_wrap_ptasks_first(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::ptask *result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptasks_first ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::ptask *)surfit::ptasks_first();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__ptask,0));
    return TCL_OK;
}

static int
_wrap_ptasks_last(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::ptask *result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptasks_last ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::ptask *)surfit::ptasks_last();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__ptask,0));
    return TCL_OK;
}

static int
_wrap_ptask_concat(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::ptask *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:ptask_concat ptsk ",&arg1,SWIGTYPE_p_surfit__ptask) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::ptask_concat(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptasks_size(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptasks_size ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (int)surfit::ptasks_size();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_ptasks_info(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int result;
    
    if (SWIG_GetArgs(interp, objc, objv,":ptasks_info ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (int)surfit::ptasks_info();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_read(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    double arg3 = 0 ;
    int arg4 = 2 ;
    int arg5 = 1 ;
    int arg6 = 2 ;
    char *arg7 = " " ;
    int arg8 = 250 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|sdiiisi:parea_read filename pareaname value columns col1 col2 delimiter grow_by ",&arg1,&arg2,&arg3,&arg4,&arg5,&arg6,&arg7,&arg8) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_read((char const *)arg1,(char const *)arg2,arg3,arg4,arg5,arg6,(char const *)arg7,arg8);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_write(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"ss:parea_write filename delimiter ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_write((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_save(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"ss:parea_save filename pareaname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_save((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_load(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"ss:parea_load filename pareaname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_load((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_unload(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":parea_unload ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_unload();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_to_curv(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":parea_to_curv ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_to_curv();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_to_curvs(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":parea_to_curvs ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_to_curvs();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_to_pareas(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":parea_to_pareas ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_to_pareas();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_check(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":parea_check ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_check();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_value(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:parea_value value ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_value(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_set(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::parea *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:parea_set par ",&arg1,SWIGTYPE_p_surfit__parea) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_set(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_parea_name(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 = NULL ;
    surfit::parea *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"|sp:parea_name new_name ar ",&arg1,&arg2,SWIGTYPE_p_surfit__parea) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::parea_name((char const *)arg1,arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pareas_delall(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pareas_delall ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pareas_delall();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pareas_del(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"i:pareas_del pos ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pareas_del(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pareas_read(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    double arg3 = 0 ;
    int arg4 = 2 ;
    int arg5 = 1 ;
    int arg6 = 2 ;
    char *arg7 = " " ;
    int arg8 = 250 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|sdiiisi:pareas_read filename pareaname value columns col1 col2 delimiter grow_by ",&arg1,&arg2,&arg3,&arg4,&arg5,&arg6,&arg7,&arg8) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pareas_read((char const *)arg1,(char const *)arg2,arg3,arg4,arg5,arg6,(char const *)arg7,arg8);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pareas_load(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"ss:pareas_load filename pareaname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pareas_load((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pareas_at(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int arg1 ;
    surfit::parea *result;
    
    if (SWIG_GetArgs(interp, objc, objv,"i:pareas_at pos ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::parea *)surfit::pareas_at(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__parea,0));
    return TCL_OK;
}

static int
_wrap_pareas_first(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::parea *result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pareas_first ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::parea *)surfit::pareas_first();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__parea,0));
    return TCL_OK;
}

static int
_wrap_pareas_last(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::parea *result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pareas_last ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::parea *)surfit::pareas_last();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__parea,0));
    return TCL_OK;
}

static int
_wrap_pareas_size(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pareas_size ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (int)surfit::pareas_size();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pareas_info(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pareas_info ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (int)surfit::pareas_info();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_read(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    int arg3 = 3 ;
    int arg4 = 1 ;
    int arg5 = 2 ;
    int arg6 = 3 ;
    char *arg7 = " " ;
    int arg8 = 250 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|siiiisi:pcntr_read filename pcntrname columns col1 col2 col3 delimiter grow_by ",&arg1,&arg2,&arg3,&arg4,&arg5,&arg6,&arg7,&arg8) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_read((char const *)arg1,(char const *)arg2,arg3,arg4,arg5,arg6,(char const *)arg7,arg8);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_write(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = " " ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|s:pcntr_write filename delimiter ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_write((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_save(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|s:pcntr_save filename pcntrname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_save((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_load(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|s:pcntr_load filename pcntrname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_load((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_unload(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pcntr_unload ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_unload();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_plus_real(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:pcntr_plus_real value ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_plus_real(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_minus_real(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:pcntr_minus_real value ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_minus_real(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_mult_real(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:pcntr_mult_real value ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_mult_real(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_div_real(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:pcntr_div_real value ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_div_real(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_to_curv(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pcntr_to_curv ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_to_curv();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_to_curvs(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pcntr_to_curvs ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_to_curvs();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_to_pcntrs(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pcntr_to_pcntrs ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_to_pcntrs();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_check(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pcntr_check ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_check();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_set(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::pcntr *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:pcntr_set pcontour ",&arg1,SWIGTYPE_p_surfit__pcntr) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_set(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntr_name(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 = NULL ;
    surfit::pcntr *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"|sp:pcntr_name new_name contour ",&arg1,&arg2,SWIGTYPE_p_surfit__pcntr) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntr_name((char const *)arg1,arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntrs_delall(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pcntrs_delall ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntrs_delall();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntrs_del(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"i:pcntrs_del pos ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntrs_del(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntrs_read(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    int arg3 = 3 ;
    int arg4 = 1 ;
    int arg5 = 2 ;
    int arg6 = 3 ;
    char *arg7 = " " ;
    int arg8 = 250 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|siiiisi:pcntrs_read filename pcntrname columns col1 col2 col3 delimiter grow_by ",&arg1,&arg2,&arg3,&arg4,&arg5,&arg6,&arg7,&arg8) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntrs_read((char const *)arg1,(char const *)arg2,arg3,arg4,arg5,arg6,(char const *)arg7,arg8);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntrs_load(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|s:pcntrs_load filename pcntrname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pcntrs_load((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntrs_at(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int arg1 ;
    surfit::pcntr *result;
    
    if (SWIG_GetArgs(interp, objc, objv,"i:pcntrs_at pos ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::pcntr *)surfit::pcntrs_at(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__pcntr,0));
    return TCL_OK;
}

static int
_wrap_pcntrs_first(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::pcntr *result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pcntrs_first ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::pcntr *)surfit::pcntrs_first();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__pcntr,0));
    return TCL_OK;
}

static int
_wrap_pcntrs_last(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::pcntr *result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pcntrs_last ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::pcntr *)surfit::pcntrs_last();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__pcntr,0));
    return TCL_OK;
}

static int
_wrap_pcntrs_size(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pcntrs_size ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (int)surfit::pcntrs_size();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pcntrs_info(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pcntrs_info ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (int)surfit::pcntrs_info();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_read(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    double arg3 = 0 ;
    int arg4 = 2 ;
    int arg5 = 1 ;
    int arg6 = 2 ;
    char *arg7 = " " ;
    int arg8 = 250 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|sdiiisi:piso_read filename pisoname value columns col1 col2 delimiter grow_by ",&arg1,&arg2,&arg3,&arg4,&arg5,&arg6,&arg7,&arg8) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_read((char const *)arg1,(char const *)arg2,arg3,arg4,arg5,arg6,(char const *)arg7,arg8);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_write(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"ss:piso_write filename delimiter ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_write((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_save(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"ss:piso_save filename pisoname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_save((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_load(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"ss:piso_load filename pisoname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_load((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_unload(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":piso_unload ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_unload();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_to_curv(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":piso_to_curv ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_to_curv();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_to_curvs(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":piso_to_curvs ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_to_curvs();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_to_pisos(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":piso_to_pisos ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_to_pisos();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_check(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":piso_check ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_check();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_value(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"d:piso_value value ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_value(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_set(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::piso *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"p:piso_set pisoline ",&arg1,SWIGTYPE_p_surfit__piso) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_set(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_piso_name(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 = NULL ;
    surfit::piso *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"|sp:piso_name new_name pisoline ",&arg1,&arg2,SWIGTYPE_p_surfit__piso) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::piso_name((char const *)arg1,arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pisos_delall(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pisos_delall ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pisos_delall();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pisos_del(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"i:pisos_del pos ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pisos_del(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pisos_read(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    double arg3 = 0 ;
    int arg4 = 2 ;
    int arg5 = 1 ;
    int arg6 = 2 ;
    char *arg7 = " " ;
    int arg8 = 250 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|sdiiisi:pisos_read filename pisoname value columns col1 col2 delimiter grow_by ",&arg1,&arg2,&arg3,&arg4,&arg5,&arg6,&arg7,&arg8) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pisos_read((char const *)arg1,(char const *)arg2,arg3,arg4,arg5,arg6,(char const *)arg7,arg8);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pisos_load(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"ss:pisos_load filename pisoname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::pisos_load((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pisos_at(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int arg1 ;
    surfit::piso *result;
    
    if (SWIG_GetArgs(interp, objc, objv,"i:pisos_at pos ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::piso *)surfit::pisos_at(arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__piso,0));
    return TCL_OK;
}

static int
_wrap_pisos_first(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::piso *result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pisos_first ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::piso *)surfit::pisos_first();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__piso,0));
    return TCL_OK;
}

static int
_wrap_pisos_last(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    surfit::piso *result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pisos_last ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (surfit::piso *)surfit::pisos_last();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,SWIG_NewPointerObj((void *) result, SWIGTYPE_p_surfit__piso,0));
    return TCL_OK;
}

static int
_wrap_pisos_size(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pisos_size ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (int)surfit::pisos_size();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_pisos_info(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int result;
    
    if (SWIG_GetArgs(interp, objc, objv,":pisos_info ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (int)surfit::pisos_info();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_lcm_simple_set(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 = 1 ;
    double arg2 = 1 ;
    double arg3 = 1 ;
    
    if (SWIG_GetArgs(interp, objc, objv,"|ddd:lcm_simple_set permeability viscosity multiplier ",&arg1,&arg2,&arg3) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            surfit::lcm_simple_set(arg1,arg2,arg3);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    
    return TCL_OK;
}

static int
_wrap_lcm_simple_unload(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":lcm_simple_unload ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::lcm_simple_unload();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_lcm_simple_read(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"ss:lcm_simple_read filename lcm_simple_name ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::lcm_simple_read((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_lcm_simple_load(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|s:lcm_simple_load filename lcm_simple_name ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::lcm_simple_load((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_lcm_simple_write(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s:lcm_simple_write filename ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::lcm_simple_write((char const *)arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_lcm_simple_save(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    char *arg2 = NULL ;
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,"s|s:lcm_simple_save filename lcm_simple_name ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::lcm_simple_save((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_lcm_simple_check(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    bool result;
    
    if (SWIG_GetArgs(interp, objc, objv,":lcm_simple_check ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (bool)surfit::lcm_simple_check();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_lcm_simple_info(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    int result;
    
    if (SWIG_GetArgs(interp, objc, objv,":lcm_simple_info ") == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (int)surfit::lcm_simple_info();
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewIntObj((long) result));
    return TCL_OK;
}

static int
_wrap_func_debit(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    double arg2 ;
    double result;
    
    if (SWIG_GetArgs(interp, objc, objv,"dd:func_debit x y ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (double)surfit::func_debit(arg1,arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewDoubleObj((double) result));
    return TCL_OK;
}

static int
_wrap_func_debit_rect(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    double arg1 ;
    double arg2 ;
    double arg3 ;
    double arg4 ;
    double result;
    
    if (SWIG_GetArgs(interp, objc, objv,"dddd:func_debit_rect x1 y1 x2 y2 ",&arg1,&arg2,&arg3,&arg4) == TCL_ERROR) return TCL_ERROR;
    {
        try {
            result = (double)surfit::func_debit_rect(arg1,arg2,arg3,arg4);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
    }
    Tcl_SetObjResult(interp,Tcl_NewDoubleObj((double) result));
    return TCL_OK;
}

static char *_wrap_ptask_color_red_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::ptask_color_red);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_ptask_color_red_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::ptask_color_red = (int) temp;
    }
    return NULL;
}


static char *_wrap_ptask_color_green_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::ptask_color_green);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_ptask_color_green_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::ptask_color_green = (int) temp;
    }
    return NULL;
}


static char *_wrap_ptask_color_blue_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::ptask_color_blue);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_ptask_color_blue_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::ptask_color_blue = (int) temp;
    }
    return NULL;
}


static char *_wrap_ptask_color_alpha_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::ptask_color_alpha);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_ptask_color_alpha_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::ptask_color_alpha = (int) temp;
    }
    return NULL;
}


static char *_wrap_draw_ptask_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::draw_ptask);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_draw_ptask_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::draw_ptask = (int) temp;
    }
    return NULL;
}


static char *_wrap_parea_color_red_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::parea_color_red);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_parea_color_red_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::parea_color_red = (int) temp;
    }
    return NULL;
}


static char *_wrap_parea_color_green_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::parea_color_green);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_parea_color_green_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::parea_color_green = (int) temp;
    }
    return NULL;
}


static char *_wrap_parea_color_blue_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::parea_color_blue);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_parea_color_blue_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::parea_color_blue = (int) temp;
    }
    return NULL;
}


static char *_wrap_parea_color_alpha_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::parea_color_alpha);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_parea_color_alpha_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::parea_color_alpha = (int) temp;
    }
    return NULL;
}


static char *_wrap_draw_parea_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::draw_parea);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_draw_parea_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::draw_parea = (int) temp;
    }
    return NULL;
}


static char *_wrap_piso_color_red_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::piso_color_red);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_piso_color_red_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::piso_color_red = (int) temp;
    }
    return NULL;
}


static char *_wrap_piso_color_green_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::piso_color_green);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_piso_color_green_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::piso_color_green = (int) temp;
    }
    return NULL;
}


static char *_wrap_piso_color_blue_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::piso_color_blue);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_piso_color_blue_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::piso_color_blue = (int) temp;
    }
    return NULL;
}


static char *_wrap_piso_color_alpha_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::piso_color_alpha);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_piso_color_alpha_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::piso_color_alpha = (int) temp;
    }
    return NULL;
}


static char *_wrap_draw_piso_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::draw_piso);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_draw_piso_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::draw_piso = (int) temp;
    }
    return NULL;
}


static char *_wrap_pcntr_color_red_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::pcntr_color_red);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_pcntr_color_red_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::pcntr_color_red = (int) temp;
    }
    return NULL;
}


static char *_wrap_pcntr_color_green_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::pcntr_color_green);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_pcntr_color_green_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::pcntr_color_green = (int) temp;
    }
    return NULL;
}


static char *_wrap_pcntr_color_blue_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::pcntr_color_blue);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_pcntr_color_blue_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::pcntr_color_blue = (int) temp;
    }
    return NULL;
}


static char *_wrap_pcntr_color_alpha_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::pcntr_color_alpha);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_pcntr_color_alpha_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::pcntr_color_alpha = (int) temp;
    }
    return NULL;
}


static char *_wrap_draw_pcntr_get(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    
    value = Tcl_NewIntObj((long) surfit::draw_pcntr);
    if (value) {
        Tcl_SetVar2(interp,name1,name2,Tcl_GetStringFromObj(value,NULL), flags);
        Tcl_DecrRefCount(value);
    }
    return NULL;
}


static char *_wrap_draw_pcntr_set(ClientData clientData, Tcl_Interp *interp, char *name1, char *name2, int flags) {
    Tcl_Obj *value = 0;
    Tcl_Obj *name1o = 0;
    
    name1o = Tcl_NewStringObj(name1,-1);
    value = Tcl_ObjGetVar2(interp, name1o, 0, flags);
    Tcl_DecrRefCount(name1o);
    if (!value) return NULL;
    {
        long temp;
        if (Tcl_GetLongFromObj(interp, value, &temp) != TCL_OK) {
            return (char*) "Type error. expected an integer";
        }
        surfit::draw_pcntr = (int) temp;
    }
    return NULL;
}


static int
_wrap_ptask_color(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    
    if (SWIG_GetArgs(interp, objc, objv,"s:ptask_color color_name ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        #ifdef HAVE_GL_WORKS
        try {
            surfit::ptask_color((char const *)arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
        #else
        return TCL_ERROR;
        #endif
    }
    
    return TCL_OK;
}

static int
_wrap_ptask_show(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 = NULL ;
    char *arg2 = NULL ;
    
    if (SWIG_GetArgs(interp, objc, objv,"|ss:ptask_show filename ptaskname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        #ifdef HAVE_GL_WORKS
        try {
            surfit::ptask_show((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
        #else
        return TCL_ERROR;
        #endif
    }
    
    return TCL_OK;
}

static int
_wrap_parea_color(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    
    if (SWIG_GetArgs(interp, objc, objv,"s:parea_color color_name ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        #ifdef HAVE_GL_WORKS
        try {
            surfit::parea_color((char const *)arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
        #else
        return TCL_ERROR;
        #endif
    }
    
    return TCL_OK;
}

static int
_wrap_parea_show(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 = NULL ;
    char *arg2 = NULL ;
    
    if (SWIG_GetArgs(interp, objc, objv,"|ss:parea_show filename pareaname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        #ifdef HAVE_GL_WORKS
        try {
            surfit::parea_show((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
        #else
        return TCL_ERROR;
        #endif
    }
    
    return TCL_OK;
}

static int
_wrap_piso_color(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    
    if (SWIG_GetArgs(interp, objc, objv,"s:piso_color color_name ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        #ifdef HAVE_GL_WORKS
        try {
            surfit::piso_color((char const *)arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
        #else
        return TCL_ERROR;
        #endif
    }
    
    return TCL_OK;
}

static int
_wrap_piso_show(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 = NULL ;
    char *arg2 = NULL ;
    
    if (SWIG_GetArgs(interp, objc, objv,"|ss:piso_show filename pisoname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        #ifdef HAVE_GL_WORKS
        try {
            surfit::piso_show((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
        #else
        return TCL_ERROR;
        #endif
    }
    
    return TCL_OK;
}

static int
_wrap_pcntr_color(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 ;
    
    if (SWIG_GetArgs(interp, objc, objv,"s:pcntr_color color_name ",&arg1) == TCL_ERROR) return TCL_ERROR;
    {
        #ifdef HAVE_GL_WORKS
        try {
            surfit::pcntr_color((char const *)arg1);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
        #else
        return TCL_ERROR;
        #endif
    }
    
    return TCL_OK;
}

static int
_wrap_pcntr_show(ClientData clientData, Tcl_Interp *interp, int objc, Tcl_Obj *CONST objv[]) {
    char *arg1 = NULL ;
    char *arg2 = NULL ;
    
    if (SWIG_GetArgs(interp, objc, objv,"|ss:pcntr_show filename pcntrname ",&arg1,&arg2) == TCL_ERROR) return TCL_ERROR;
    {
        #ifdef HAVE_GL_WORKS
        try {
            surfit::pcntr_show((char const *)arg1,(char const *)arg2);
            
        }
        catch(...) {
            return TCL_ERROR;
        }
        #else
        return TCL_ERROR;
        #endif
    }
    
    return TCL_OK;
}


static swig_command_info swig_commands[] = {
    { SWIG_prefix "flow", (swig_wrapper_func) _wrap_flow, NULL},
    { SWIG_prefix "ptask_load", (swig_wrapper_func) _wrap_ptask_load, NULL},
    { SWIG_prefix "ptask_read", (swig_wrapper_func) _wrap_ptask_read, NULL},
    { SWIG_prefix "ptask_unload", (swig_wrapper_func) _wrap_ptask_unload, NULL},
    { SWIG_prefix "ptask_save", (swig_wrapper_func) _wrap_ptask_save, NULL},
    { SWIG_prefix "ptask_write", (swig_wrapper_func) _wrap_ptask_write, NULL},
    { SWIG_prefix "ptask_transform", (swig_wrapper_func) _wrap_ptask_transform, NULL},
    { SWIG_prefix "ptask_inverse_transform", (swig_wrapper_func) _wrap_ptask_inverse_transform, NULL},
    { SWIG_prefix "ptask_filter_by_defarea", (swig_wrapper_func) _wrap_ptask_filter_by_defarea, NULL},
    { SWIG_prefix "ptask_filter_by_func", (swig_wrapper_func) _wrap_ptask_filter_by_func, NULL},
    { SWIG_prefix "ptask_update_by_func", (swig_wrapper_func) _wrap_ptask_update_by_func, NULL},
    { SWIG_prefix "ptask_minx", (swig_wrapper_func) _wrap_ptask_minx, NULL},
    { SWIG_prefix "ptask_maxx", (swig_wrapper_func) _wrap_ptask_maxx, NULL},
    { SWIG_prefix "ptask_miny", (swig_wrapper_func) _wrap_ptask_miny, NULL},
    { SWIG_prefix "ptask_maxy", (swig_wrapper_func) _wrap_ptask_maxy, NULL},
    { SWIG_prefix "ptask_minz", (swig_wrapper_func) _wrap_ptask_minz, NULL},
    { SWIG_prefix "ptask_maxz", (swig_wrapper_func) _wrap_ptask_maxz, NULL},
    { SWIG_prefix "ptask_add_noise", (swig_wrapper_func) _wrap_ptask_add_noise, NULL},
    { SWIG_prefix "ptask_mean", (swig_wrapper_func) _wrap_ptask_mean, NULL},
    { SWIG_prefix "ptask_std", (swig_wrapper_func) _wrap_ptask_std, NULL},
    { SWIG_prefix "ptask_plus", (swig_wrapper_func) _wrap_ptask_plus, NULL},
    { SWIG_prefix "ptask_minus", (swig_wrapper_func) _wrap_ptask_minus, NULL},
    { SWIG_prefix "ptask_mult", (swig_wrapper_func) _wrap_ptask_mult, NULL},
    { SWIG_prefix "ptask_div", (swig_wrapper_func) _wrap_ptask_div, NULL},
    { SWIG_prefix "ptask_set", (swig_wrapper_func) _wrap_ptask_set, NULL},
    { SWIG_prefix "ptask_plus_real", (swig_wrapper_func) _wrap_ptask_plus_real, NULL},
    { SWIG_prefix "ptask_minus_real", (swig_wrapper_func) _wrap_ptask_minus_real, NULL},
    { SWIG_prefix "ptask_mult_real", (swig_wrapper_func) _wrap_ptask_mult_real, NULL},
    { SWIG_prefix "ptask_div_real", (swig_wrapper_func) _wrap_ptask_div_real, NULL},
    { SWIG_prefix "ptask_set_real", (swig_wrapper_func) _wrap_ptask_set_real, NULL},
    { SWIG_prefix "ptask_to_task", (swig_wrapper_func) _wrap_ptask_to_task, NULL},
    { SWIG_prefix "ptask_to_tasks", (swig_wrapper_func) _wrap_ptask_to_tasks, NULL},
    { SWIG_prefix "task_to_ptask", (swig_wrapper_func) _wrap_task_to_ptask, NULL},
    { SWIG_prefix "task_to_ptasks", (swig_wrapper_func) _wrap_task_to_ptasks, NULL},
    { SWIG_prefix "ptask_to_ptasks", (swig_wrapper_func) _wrap_ptask_to_ptasks, NULL},
    { SWIG_prefix "ptask_check", (swig_wrapper_func) _wrap_ptask_check, NULL},
    { SWIG_prefix "ptask_size", (swig_wrapper_func) _wrap_ptask_size, NULL},
    { SWIG_prefix "ptask_name", (swig_wrapper_func) _wrap_ptask_name, NULL},
    { SWIG_prefix "ptasks_load", (swig_wrapper_func) _wrap_ptasks_load, NULL},
    { SWIG_prefix "ptasks_read", (swig_wrapper_func) _wrap_ptasks_read, NULL},
    { SWIG_prefix "ptasks_delall", (swig_wrapper_func) _wrap_ptasks_delall, NULL},
    { SWIG_prefix "ptasks_del", (swig_wrapper_func) _wrap_ptasks_del, NULL},
    { SWIG_prefix "ptasks_at", (swig_wrapper_func) _wrap_ptasks_at, NULL},
    { SWIG_prefix "ptasks_first", (swig_wrapper_func) _wrap_ptasks_first, NULL},
    { SWIG_prefix "ptasks_last", (swig_wrapper_func) _wrap_ptasks_last, NULL},
    { SWIG_prefix "ptask_concat", (swig_wrapper_func) _wrap_ptask_concat, NULL},
    { SWIG_prefix "ptasks_size", (swig_wrapper_func) _wrap_ptasks_size, NULL},
    { SWIG_prefix "ptasks_info", (swig_wrapper_func) _wrap_ptasks_info, NULL},
    { SWIG_prefix "parea_read", (swig_wrapper_func) _wrap_parea_read, NULL},
    { SWIG_prefix "parea_write", (swig_wrapper_func) _wrap_parea_write, NULL},
    { SWIG_prefix "parea_save", (swig_wrapper_func) _wrap_parea_save, NULL},
    { SWIG_prefix "parea_load", (swig_wrapper_func) _wrap_parea_load, NULL},
    { SWIG_prefix "parea_unload", (swig_wrapper_func) _wrap_parea_unload, NULL},
    { SWIG_prefix "parea_to_curv", (swig_wrapper_func) _wrap_parea_to_curv, NULL},
    { SWIG_prefix "parea_to_curvs", (swig_wrapper_func) _wrap_parea_to_curvs, NULL},
    { SWIG_prefix "parea_to_pareas", (swig_wrapper_func) _wrap_parea_to_pareas, NULL},
    { SWIG_prefix "parea_check", (swig_wrapper_func) _wrap_parea_check, NULL},
    { SWIG_prefix "parea_value", (swig_wrapper_func) _wrap_parea_value, NULL},
    { SWIG_prefix "parea_set", (swig_wrapper_func) _wrap_parea_set, NULL},
    { SWIG_prefix "parea_name", (swig_wrapper_func) _wrap_parea_name, NULL},
    { SWIG_prefix "pareas_delall", (swig_wrapper_func) _wrap_pareas_delall, NULL},
    { SWIG_prefix "pareas_del", (swig_wrapper_func) _wrap_pareas_del, NULL},
    { SWIG_prefix "pareas_read", (swig_wrapper_func) _wrap_pareas_read, NULL},
    { SWIG_prefix "pareas_load", (swig_wrapper_func) _wrap_pareas_load, NULL},
    { SWIG_prefix "pareas_at", (swig_wrapper_func) _wrap_pareas_at, NULL},
    { SWIG_prefix "pareas_first", (swig_wrapper_func) _wrap_pareas_first, NULL},
    { SWIG_prefix "pareas_last", (swig_wrapper_func) _wrap_pareas_last, NULL},
    { SWIG_prefix "pareas_size", (swig_wrapper_func) _wrap_pareas_size, NULL},
    { SWIG_prefix "pareas_info", (swig_wrapper_func) _wrap_pareas_info, NULL},
    { SWIG_prefix "pcntr_read", (swig_wrapper_func) _wrap_pcntr_read, NULL},
    { SWIG_prefix "pcntr_write", (swig_wrapper_func) _wrap_pcntr_write, NULL},
    { SWIG_prefix "pcntr_save", (swig_wrapper_func) _wrap_pcntr_save, NULL},
    { SWIG_prefix "pcntr_load", (swig_wrapper_func) _wrap_pcntr_load, NULL},
    { SWIG_prefix "pcntr_unload", (swig_wrapper_func) _wrap_pcntr_unload, NULL},
    { SWIG_prefix "pcntr_plus_real", (swig_wrapper_func) _wrap_pcntr_plus_real, NULL},
    { SWIG_prefix "pcntr_minus_real", (swig_wrapper_func) _wrap_pcntr_minus_real, NULL},
    { SWIG_prefix "pcntr_mult_real", (swig_wrapper_func) _wrap_pcntr_mult_real, NULL},
    { SWIG_prefix "pcntr_div_real", (swig_wrapper_func) _wrap_pcntr_div_real, NULL},
    { SWIG_prefix "pcntr_to_curv", (swig_wrapper_func) _wrap_pcntr_to_curv, NULL},
    { SWIG_prefix "pcntr_to_curvs", (swig_wrapper_func) _wrap_pcntr_to_curvs, NULL},
    { SWIG_prefix "pcntr_to_pcntrs", (swig_wrapper_func) _wrap_pcntr_to_pcntrs, NULL},
    { SWIG_prefix "pcntr_check", (swig_wrapper_func) _wrap_pcntr_check, NULL},
    { SWIG_prefix "pcntr_set", (swig_wrapper_func) _wrap_pcntr_set, NULL},
    { SWIG_prefix "pcntr_name", (swig_wrapper_func) _wrap_pcntr_name, NULL},
    { SWIG_prefix "pcntrs_delall", (swig_wrapper_func) _wrap_pcntrs_delall, NULL},
    { SWIG_prefix "pcntrs_del", (swig_wrapper_func) _wrap_pcntrs_del, NULL},
    { SWIG_prefix "pcntrs_read", (swig_wrapper_func) _wrap_pcntrs_read, NULL},
    { SWIG_prefix "pcntrs_load", (swig_wrapper_func) _wrap_pcntrs_load, NULL},
    { SWIG_prefix "pcntrs_at", (swig_wrapper_func) _wrap_pcntrs_at, NULL},
    { SWIG_prefix "pcntrs_first", (swig_wrapper_func) _wrap_pcntrs_first, NULL},
    { SWIG_prefix "pcntrs_last", (swig_wrapper_func) _wrap_pcntrs_last, NULL},
    { SWIG_prefix "pcntrs_size", (swig_wrapper_func) _wrap_pcntrs_size, NULL},
    { SWIG_prefix "pcntrs_info", (swig_wrapper_func) _wrap_pcntrs_info, NULL},
    { SWIG_prefix "piso_read", (swig_wrapper_func) _wrap_piso_read, NULL},
    { SWIG_prefix "piso_write", (swig_wrapper_func) _wrap_piso_write, NULL},
    { SWIG_prefix "piso_save", (swig_wrapper_func) _wrap_piso_save, NULL},
    { SWIG_prefix "piso_load", (swig_wrapper_func) _wrap_piso_load, NULL},
    { SWIG_prefix "piso_unload", (swig_wrapper_func) _wrap_piso_unload, NULL},
    { SWIG_prefix "piso_to_curv", (swig_wrapper_func) _wrap_piso_to_curv, NULL},
    { SWIG_prefix "piso_to_curvs", (swig_wrapper_func) _wrap_piso_to_curvs, NULL},
    { SWIG_prefix "piso_to_pisos", (swig_wrapper_func) _wrap_piso_to_pisos, NULL},
    { SWIG_prefix "piso_check", (swig_wrapper_func) _wrap_piso_check, NULL},
    { SWIG_prefix "piso_value", (swig_wrapper_func) _wrap_piso_value, NULL},
    { SWIG_prefix "piso_set", (swig_wrapper_func) _wrap_piso_set, NULL},
    { SWIG_prefix "piso_name", (swig_wrapper_func) _wrap_piso_name, NULL},
    { SWIG_prefix "pisos_delall", (swig_wrapper_func) _wrap_pisos_delall, NULL},
    { SWIG_prefix "pisos_del", (swig_wrapper_func) _wrap_pisos_del, NULL},
    { SWIG_prefix "pisos_read", (swig_wrapper_func) _wrap_pisos_read, NULL},
    { SWIG_prefix "pisos_load", (swig_wrapper_func) _wrap_pisos_load, NULL},
    { SWIG_prefix "pisos_at", (swig_wrapper_func) _wrap_pisos_at, NULL},
    { SWIG_prefix "pisos_first", (swig_wrapper_func) _wrap_pisos_first, NULL},
    { SWIG_prefix "pisos_last", (swig_wrapper_func) _wrap_pisos_last, NULL},
    { SWIG_prefix "pisos_size", (swig_wrapper_func) _wrap_pisos_size, NULL},
    { SWIG_prefix "pisos_info", (swig_wrapper_func) _wrap_pisos_info, NULL},
    { SWIG_prefix "lcm_simple_set", (swig_wrapper_func) _wrap_lcm_simple_set, NULL},
    { SWIG_prefix "lcm_simple_unload", (swig_wrapper_func) _wrap_lcm_simple_unload, NULL},
    { SWIG_prefix "lcm_simple_read", (swig_wrapper_func) _wrap_lcm_simple_read, NULL},
    { SWIG_prefix "lcm_simple_load", (swig_wrapper_func) _wrap_lcm_simple_load, NULL},
    { SWIG_prefix "lcm_simple_write", (swig_wrapper_func) _wrap_lcm_simple_write, NULL},
    { SWIG_prefix "lcm_simple_save", (swig_wrapper_func) _wrap_lcm_simple_save, NULL},
    { SWIG_prefix "lcm_simple_check", (swig_wrapper_func) _wrap_lcm_simple_check, NULL},
    { SWIG_prefix "lcm_simple_info", (swig_wrapper_func) _wrap_lcm_simple_info, NULL},
    { SWIG_prefix "func_debit", (swig_wrapper_func) _wrap_func_debit, NULL},
    { SWIG_prefix "func_debit_rect", (swig_wrapper_func) _wrap_func_debit_rect, NULL},
    { SWIG_prefix "ptask_color", (swig_wrapper_func) _wrap_ptask_color, NULL},
    { SWIG_prefix "ptask_show", (swig_wrapper_func) _wrap_ptask_show, NULL},
    { SWIG_prefix "parea_color", (swig_wrapper_func) _wrap_parea_color, NULL},
    { SWIG_prefix "parea_show", (swig_wrapper_func) _wrap_parea_show, NULL},
    { SWIG_prefix "piso_color", (swig_wrapper_func) _wrap_piso_color, NULL},
    { SWIG_prefix "piso_show", (swig_wrapper_func) _wrap_piso_show, NULL},
    { SWIG_prefix "pcntr_color", (swig_wrapper_func) _wrap_pcntr_color, NULL},
    { SWIG_prefix "pcntr_show", (swig_wrapper_func) _wrap_pcntr_show, NULL},
    {0, 0, 0}
};

static swig_var_info swig_variables[] = {
    { SWIG_prefix "ptask_color_red", 0, (swig_variable_func) _wrap_ptask_color_red_get,(swig_variable_func) _wrap_ptask_color_red_set},
    { SWIG_prefix "ptask_color_green", 0, (swig_variable_func) _wrap_ptask_color_green_get,(swig_variable_func) _wrap_ptask_color_green_set},
    { SWIG_prefix "ptask_color_blue", 0, (swig_variable_func) _wrap_ptask_color_blue_get,(swig_variable_func) _wrap_ptask_color_blue_set},
    { SWIG_prefix "ptask_color_alpha", 0, (swig_variable_func) _wrap_ptask_color_alpha_get,(swig_variable_func) _wrap_ptask_color_alpha_set},
    { SWIG_prefix "draw_ptask", 0, (swig_variable_func) _wrap_draw_ptask_get,(swig_variable_func) _wrap_draw_ptask_set},
    { SWIG_prefix "parea_color_red", 0, (swig_variable_func) _wrap_parea_color_red_get,(swig_variable_func) _wrap_parea_color_red_set},
    { SWIG_prefix "parea_color_green", 0, (swig_variable_func) _wrap_parea_color_green_get,(swig_variable_func) _wrap_parea_color_green_set},
    { SWIG_prefix "parea_color_blue", 0, (swig_variable_func) _wrap_parea_color_blue_get,(swig_variable_func) _wrap_parea_color_blue_set},
    { SWIG_prefix "parea_color_alpha", 0, (swig_variable_func) _wrap_parea_color_alpha_get,(swig_variable_func) _wrap_parea_color_alpha_set},
    { SWIG_prefix "draw_parea", 0, (swig_variable_func) _wrap_draw_parea_get,(swig_variable_func) _wrap_draw_parea_set},
    { SWIG_prefix "piso_color_red", 0, (swig_variable_func) _wrap_piso_color_red_get,(swig_variable_func) _wrap_piso_color_red_set},
    { SWIG_prefix "piso_color_green", 0, (swig_variable_func) _wrap_piso_color_green_get,(swig_variable_func) _wrap_piso_color_green_set},
    { SWIG_prefix "piso_color_blue", 0, (swig_variable_func) _wrap_piso_color_blue_get,(swig_variable_func) _wrap_piso_color_blue_set},
    { SWIG_prefix "piso_color_alpha", 0, (swig_variable_func) _wrap_piso_color_alpha_get,(swig_variable_func) _wrap_piso_color_alpha_set},
    { SWIG_prefix "draw_piso", 0, (swig_variable_func) _wrap_draw_piso_get,(swig_variable_func) _wrap_draw_piso_set},
    { SWIG_prefix "pcntr_color_red", 0, (swig_variable_func) _wrap_pcntr_color_red_get,(swig_variable_func) _wrap_pcntr_color_red_set},
    { SWIG_prefix "pcntr_color_green", 0, (swig_variable_func) _wrap_pcntr_color_green_get,(swig_variable_func) _wrap_pcntr_color_green_set},
    { SWIG_prefix "pcntr_color_blue", 0, (swig_variable_func) _wrap_pcntr_color_blue_get,(swig_variable_func) _wrap_pcntr_color_blue_set},
    { SWIG_prefix "pcntr_color_alpha", 0, (swig_variable_func) _wrap_pcntr_color_alpha_get,(swig_variable_func) _wrap_pcntr_color_alpha_set},
    { SWIG_prefix "draw_pcntr", 0, (swig_variable_func) _wrap_draw_pcntr_get,(swig_variable_func) _wrap_draw_pcntr_set},
    {0,0,0,0}
};

static swig_const_info swig_constants[] = {
{ SWIG_TCL_STRING,  (char*)"REAL_NAME", 0, 0, (void *)"double", 0},
    {0,0,0,0,0,0}
};

/* -------- TYPE CONVERSION AND EQUIVALENCE RULES (BEGIN) -------- */

static swig_type_info _swigt__p_surfit__ptask[] = {{"_p_surfit__ptask", 0, "surfit::ptask *"},{"_p_surfit__ptask"},{0}};
static swig_type_info _swigt__p_surfit__parea[] = {{"_p_surfit__parea", 0, "surfit::parea *"},{"_p_surfit__parea"},{0}};
static swig_type_info _swigt__p_surfit__piso[] = {{"_p_surfit__piso", 0, "surfit::piso *"},{"_p_surfit__piso"},{0}};
static swig_type_info _swigt__p_surfit__pcntr[] = {{"_p_surfit__pcntr", 0, "surfit::pcntr *"},{"_p_surfit__pcntr"},{0}};

static swig_type_info *swig_types_initial[] = {
_swigt__p_surfit__ptask, 
_swigt__p_surfit__parea, 
_swigt__p_surfit__piso, 
_swigt__p_surfit__pcntr, 
0
};


/* -------- TYPE CONVERSION AND EQUIVALENCE RULES (END) -------- */

#ifdef __cplusplus
}
#endif

SWIGEXPORT(int) SWIG_init(Tcl_Interp *interp) {
    int i;
    static int added_types = 0;
    if (interp == 0) return TCL_ERROR;
    #ifdef USE_TCL_STUBS
    if (Tcl_InitStubs(interp, (char*)"8.1", 0) == NULL) {
        return TCL_ERROR;
    }
    #endif
    
    Tcl_PkgProvide(interp, (char*)SWIG_name, (char*)SWIG_version);
    
    #ifdef SWIG_namespace
    Tcl_Eval(interp, "namespace eval " SWIG_namespace " { }");
    #endif
    if (!added_types) {
        for (i = 0; swig_types_initial[i]; i++) {
            swig_types[i] = SWIG_TypeRegister(swig_types_initial[i]);
        }
        added_types = 1;
    }
    for (i = 0; swig_commands[i].name; i++) {
        Tcl_CreateObjCommand(interp, (char *) swig_commands[i].name, (swig_wrapper_func) swig_commands[i].wrapper, swig_commands[i].clientdata, NULL);
    }
    for (i = 0; swig_variables[i].name; i++) {
        Tcl_SetVar(interp, (char *) swig_variables[i].name, (char *) "", TCL_GLOBAL_ONLY);
        Tcl_TraceVar(interp, (char *) swig_variables[i].name, TCL_TRACE_READS | TCL_GLOBAL_ONLY, (Tcl_VarTraceProc *) swig_variables[i].get, (ClientData) swig_variables[i].addr);
        Tcl_TraceVar(interp, (char *) swig_variables[i].name, TCL_TRACE_WRITES | TCL_GLOBAL_ONLY, (Tcl_VarTraceProc *) swig_variables[i].set, (ClientData) swig_variables[i].addr);
    }
    SWIG_InstallConstants(interp, swig_constants);
    
    
    surfit::freeflow_init_variables(interp);
    
    return TCL_OK;
}

